# นำเว็บแอปตัวจริงขึ้น Render

เว็บแอป FastAPI ตัวเต็ม ไม่ใช่หน้า static
หน้า static ที่มีอยู่แล้วคือ https://teamnajaa.github.io/ironpulse-zero/ ซึ่งเป็นคนละอย่างกัน

---

## 1. ขั้นตอน

1. เข้า https://dashboard.render.com → **New** → **Web Service**
2. เลือก **Build and deploy from a Git repository** → ต่อ GitHub → เลือก `TEAMNAJAA/ironpulse-zero`
3. Render จะอ่าน `render.yaml` ให้เอง ถ้าไม่อ่าน ให้ตั้งเอง

| ช่อง | ค่า |
|---|---|
| Language / Runtime | **Docker** |
| Dockerfile Path | `./Dockerfile` |
| Docker Build Context Directory | `.` |
| Region | Singapore |
| Health Check Path | `/api/health` |
| Instance Type | Free (หรือสูงกว่า ดูข้อ 4) |

4. กด **Deploy** · build ครั้งแรกใช้เวลาราว 5–8 นาที เพราะต้องติดตั้ง ffmpeg และ OpenCV

ไม่ต้องตั้ง `PORT` เอง Render ใส่ให้อัตโนมัติและแอปอ่านจาก env var แล้ว

---

## 2. สิ่งที่อยู่ใน image

| | |
|---|---|
| ฐาน | `python:3.11-slim` ตรงกับ Python ที่ใช้พัฒนา |
| ffmpeg | ติดตั้งจาก apt แล้วชี้ `IMAGEIO_FFMPEG_EXE=/usr/bin/ffmpeg` ให้ใช้ตัวนี้แทนตัวที่มากับ `imageio-ffmpeg` เพราะรองรับ HEVC ครบกว่า |
| OpenCV | `opencv-python-headless` ไม่ต้องมี libGL ในคอนเทนเนอร์ |
| ไลบรารีทั้งหมด | ตรึงเวอร์ชันใน `ironpulse/requirements.txt` **โดยเฉพาะ scikit-learn** เพราะโมเดล IsolationForest ถูก fit ใหม่ตอนโหลด เวอร์ชันต่างกันคะแนนอาจไม่เท่าเดิม |
| baseline | `ironpulse/web/seed/baseline_seed.json` (26 KB) นำเข้าอัตโนมัติตอนคอนเทนเนอร์เริ่มทำงาน ถ้าฐานข้อมูลยังว่าง |
| ผู้ใช้ | รันเป็น user `ironpulse` ไม่ใช่ root |

**ยืนยันแล้วว่า baseline ที่นำเข้าจากไฟล์ seed ให้คะแนนเท่าเดิมทุกหลัก**
ทดสอบกับ `unbal3g_IMG_8436` ได้ score `0.6490071141` เท่ากับฐานข้อมูลต้นทางที่ทศนิยม 10 ตำแหน่ง

---

## 3. การอัปโหลดวิดีโอ 100 MB ขึ้นไป

**ฝั่งแอปรองรับอยู่แล้วและทดสอบจริงแล้ว** อัปโหลดไฟล์ 107.8 MB ผ่าน `/api/upload`
ได้ HTTP 200 ใน 1.0 วินาที และไฟล์ที่บันทึก **byte-identical** กับต้นฉบับ

กลไกที่ทำให้ทำได้
- Starlette สตรีมส่วนที่เป็นไฟล์ลงดิสก์ ไม่อมทั้งไฟล์ไว้ในหน่วยความจำ
- ตัวจัดการอ่านทีละ 1 MB แล้วเขียนลงดิสก์ทันที
- ถ้าเกินโควตาจะลบไฟล์ที่เขียนค้างแล้วตอบเป็นข้อความไทย ไม่ใช่ traceback

ปรับโควตาได้ด้วย env var `IRONPULSE_MAX_UPLOAD_MB` (ค่าเริ่มต้นใน image คือ 500)

⚠️ **ข้อจำกัดที่อยู่นอกเหนือแอป** Render มีขีดจำกัดขนาด request ของตัวเองที่ระดับ proxy
ถ้าอัปโหลดแล้วได้ error ก่อนที่ request จะถึงแอป (เช่น 413) นั่นคือขีดจำกัดของ Render ไม่ใช่ของโค้ด
ทางแก้คือย่อคลิปให้สั้นลงก่อนอัป **คลิปยาว 9 วินาทีก็พอ** เพราะระบบใช้แค่ 2000 เฟรมแรกอยู่แล้ว

---

## 4. ⚠️ ข้อจำกัดของ Free tier ที่ต้องรู้ก่อนแจก QR

| เรื่อง | ผลกระทบ |
|---|---|
| **CPU 0.1 core** | เครื่องนี้ใช้ 7.7 วินาทีต่อคลิป บน free tier อาจใช้ **หลายนาที** ต่อคลิป · ตัวแอปออกแบบเป็นงานเบื้องหลังพร้อมแถบความคืบหน้า จึงไม่ timeout แต่ผู้ชมต้องรอ |
| **RAM 512 MB** | วัดแล้วแอปใช้ peak **234 MB** ต่อการวิเคราะห์หนึ่งคลิป จึงพอ แต่ไม่เหลือมากถ้ามีคนใช้พร้อมกันหลายคน |
| **ดิสก์ชั่วคราว** | ไฟล์ที่อัปโหลดและประวัติการตรวจ **หายทุกครั้งที่ deploy ใหม่หรือ instance รีสตาร์ท** ถ้าต้องการให้ประวัติอยู่ถาวรต้องใช้ Persistent Disk ซึ่งเป็นแพลนเสียเงิน แล้วตั้ง `IRONPULSE_DATA_DIR` ไปที่ตำแหน่ง mount |
| **สปินดาวน์** | ไม่มีคนเข้า 15 นาทีจะหลับ ครั้งถัดไปที่เปิดต้องรอปลุกราว 1 นาที **ถ้าจะใช้หน้าบูธ ให้เปิดหน้าเว็บทิ้งไว้ก่อนกรรมการเดินมา** |

**ถ้าจะใช้จริงหน้าบูธ แนะนำ Starter plan** ซึ่งได้ CPU เต็มและไม่สปินดาวน์

---

## 5. ⚠️ บนเน็ตจะไม่มีคลิปสาธิต

คลิปวิดีโอไม่ได้อยู่ใน repo และไม่ได้อยู่ใน image เพราะไฟล์ละราว 100 MB
เมื่อ deploy แล้ว หน้าตรวจวินิจฉัยจะ **ไม่มีปุ่มลัดของคลิปสาธิต** และขึ้นข้อความอธิบายแทน
ผู้ชมต้องลากไฟล์วิดีโอของตัวเองมาวางจึงจะเห็นผล

ทางเลือกถ้าอยากให้กดแล้วเห็นผลทันที ต้องเลือกอย่างใดอย่างหนึ่ง **ยังไม่ได้ทำให้ เพราะแต่ละทางมีข้อแลกเปลี่ยน**

| ทาง | ได้ | เสีย |
|---|---|---|
| ก. บีบอัดคลิปให้เล็กลงแล้วใส่ใน repo | กดปุ่มแล้วเห็นผลทันที | **การบีบอัดเปลี่ยนค่าพิกเซล ผลลัพธ์จะไม่ตรงกับตัวเลขใน H4 อีกต่อไป** ต้องติดป้ายกำกับให้ชัดว่าเป็นคนละชุด และอาจติดตามมาร์กเกอร์ไม่ผ่านเลย |
| ข. เพิ่มโหมดเล่นซ้ำจาก track ที่สกัดไว้ (`tracks.npz` 2.2 MB อยู่ใน repo แล้ว) | คำนวณ feature และให้คะแนนสดจากข้อมูลจริง ไฟล์เล็ก เร็วมาก | **ข้ามขั้นตอนถอดรหัสวิดีโอ** จึงไม่ได้สาธิตความสามารถหลักของระบบ ต้องเขียนกำกับให้ชัด |
| ค. ใช้ Persistent Disk แล้วอัปคลิปขึ้นครั้งเดียว | เหมือนใช้งานจริงทุกอย่าง | เป็นแพลนเสียเงิน |

บอกได้ว่าจะเอาทางไหน แล้วผมทำต่อให้

---

## 6. ทดสอบหลัง deploy

```bash
curl https://<ชื่อ-service>.onrender.com/api/health
```

ควรได้ JSON ที่มี `detector`, `config_hash`, `core_version`
จากนั้นเปิดหน้าเว็บแล้วดูว่า dropdown มีเครื่อง `พัดลมตั้งพื้น H4` พร้อม `baseline 38 คลิป`
ถ้าขึ้นแบบนั้นแปลว่าไฟล์ seed ถูกนำเข้าเรียบร้อย

---

## 7. เปิดใช้บนเครื่องตัวเอง ไม่ต้องผ่านเน็ต

ดับเบิลคลิก **`เปิดเว็บแอป.bat`** ที่โฟลเดอร์หลัก
สคริปต์จะตรวจ Python ติดตั้งไลบรารีให้ถ้ายังไม่ครบ นำเข้า baseline ถ้ายังไม่มีฐานข้อมูล
แล้วเปิดเบราว์เซอร์ให้เองที่ `http://127.0.0.1:8765/`

macOS หรือ Linux ใช้ `./open-webapp.sh`
