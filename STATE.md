# IronPulse Zero — STATE

เอกสารสถานะโปรเจกต์ · อัปเดต 23 สิงหาคม 2569 (หลัง H4 + H6 + H5)

> ⚠️ **ขอบเขตโครงงานแคบลงแล้ว: ตรวจจับความไม่สมดุลของมวลหมุนอย่างเดียว**
> ชุดข้อมูล `Arise H4` ไม่มีฐานหลวม อุดตัน หรือเสียดสีเลย **ห้ามรายงานหรือประเมิน fault ชนิดเหล่านั้นในทุกกรณี**
> วัตถุประสงค์บนโปสเตอร์ต้องแก้ให้ตรงกับขอบเขตนี้
**อ่านไฟล์นี้จบแล้วทำงานต่อได้ทันที ไม่ต้องถามอะไรเพิ่ม** ยกเว้นข้อที่เขียนไว้ในหัวข้อ 7 ว่าค้างการตัดสินใจ

---

## 1. โครงสร้างและสภาพแวดล้อม

```
C:\Users\more_\Downloads\Arise\
  core/                  แพ็กเกจร่วม ต้นฉบับเดียว ทั้งงานวิจัยและแอปนำเข้าจากที่นี่
  synthetic_selftest/    ชุดตรวจสอบ core กับสูตรฟิสิกส์ ต้องรันซ้ำทุกครั้งที่แก้ core
  research/              งานวิจัย E1-E9 จบแล้ว
  ironpulse/             web app
    appcore/             โมดูลเฉพาะแอป (video, markers, cfgmap, pipeline, rules, calibrate)
    verify/              สคริปต์ตรวจสอบ + ผลลัพธ์
    h4/                  H4 + H6 · nested CV, ประเมินผล, รูปและตารางสำหรับโปสเตอร์
    web/                 H5 เว็บแอป FastAPI · 3 หน้า ไม่ต้องต่อเน็ต
    run.py               เปิดเว็บแอปด้วยคำสั่งเดียว
    requirements.txt     ไลบรารีตรึงเวอร์ชัน ใช้ทั้งเครื่องตัวเองและ Docker
    docker-entrypoint.sh นำเข้า baseline แล้วเปิด uvicorn ในคอนเทนเนอร์
    data/                คลิปสาธิต ไฟล์อัปโหลด และฐานข้อมูล SQLite
    calibration.json     ค่าสอบเทียบสเกลต่อ setup_id
    config.yaml
    HANDOVER_PARAMS.md   ที่มาของทุกพารามิเตอร์ อ่านคู่กับไฟล์นี้
  docs/                  หน้า static สำหรับ GitHub Pages สร้างด้วย ironpulse/web/build_pages.py
  Dockerfile             image สำหรับ Render มี ffmpeg และ opencv-headless
  render.yaml            blueprint ของ Render
  DEPLOY_RENDER.md       ขั้นตอน deploy และข้อจำกัดของ free tier
  เปิดเว็บแอป.bat        ดับเบิลคลิกเพื่อเปิดเว็บแอปบนเครื่องตัวเอง
  STATE.md               ไฟล์นี้
C:\Users\more_\Downloads\Arise H4\       ชุดข้อมูลจริง 65 คลิป (normal 40 · น้อยกว่า3กรัม 16 · 3กรัม 8 · calibration_ruler 1)
C:\Users\more_\Downloads\Arise H3 fix\   คลิปนำร่องรอบ 2 จำนวน 6 คลิป (normal_01..03 / unbal_01..03)
C:\Users\more_\Downloads\Arise H3\       ถูกลบไปแล้ว คลิปนำร่องรอบแรกไม่มีอยู่จริงอีกต่อไป
C:\Users\more_\Downloads\            สคริปต์ 01-05, SPEC, HANDOVER, PROTOCOL
```

| | |
|---|---|
| OS / Python | Windows, Python 3.11.9 |
| numpy 2.4.1 · scipy 1.17.0 · scikit-learn 1.9.0 · opencv 4.13.0 | matplotlib 3.10.8 · pyyaml 6.0.3 · imageio-ffmpeg 0.6.0 (ffmpeg 7.1) |
| GPU | ไม่ใช้ |

**วิธีนำเข้า core จากทุกที่**
```python
sys.path.insert(0, r"C:\Users\more_\Downloads\Arise")
os.environ["IRONPULSE_PROJECT_ROOT"] = r"C:\Users\more_\Downloads\Arise\ironpulse"
from core import dsp, features, detectors, evaluate
```
`core/config.py` หา project root ตามลำดับ: env var `IRONPULSE_PROJECT_ROOT` → ไล่ขึ้นจาก cwd หา `config.yaml` → fallback ไป `research/`

---

## 2. งานที่เสร็จแล้ว

| งาน | สถานะ | เกณฑ์ผ่าน |
|---|---|---|
| **งานวิจัย E1–E9** | เสร็จ | E1 gate AUC 0.9997 (ต้องการ ≥0.85) · 26 ตาราง · 25 รูป 300 dpi |
| **H1 ดึงพารามิเตอร์** | เสร็จ | ทุกค่ามีที่มาวัดจริง ไม่มีค่าเดา (รันการทดลองเพิ่ม e9, e9c เพื่อเติมช่องว่าง) |
| **H2 ย้าย core เป็นแพ็กเกจร่วม** | เสร็จ | self-test 18/18 · ผลไฟล์ **ไบต์ต่อไบต์เหมือนเดิม** · research regression identical |
| **H3 video front-end** | เสร็จ | แกน x ตรงกับ `03_vibro.py` **8/8 คลิป คลาดเคลื่อน 0.000000 Hz** |
| **Pilot check 8 คลิป** | เสร็จ | ติดตามสำเร็จ 2000/2000 ทุกคลิป ไม่มีหลุด |
| **Pilot รอบ 2 (6 คลิป มีป้ายกลุ่ม)** | เสร็จ | ติดตาม 2000/2000 ทุกคลิป · แยกกลุ่มได้ชัด อัตราส่วน r1 แกน y 5.88 เท่า · แอมพลิจูด 1x 207.8 เท่า |
| **แก้บั๊กจากผล pilot2 + สอบเทียบสเกล** | เสร็จ | บั๊ก G–N ในหัวข้อ 6 · core ไม่ถูกแตะ md5 ตรงเดิมทั้ง 11 ไฟล์ |
| **H4 เลือกโมเดลบนวิดีโอจริง** | เสร็จ | nested CV บน `Arise H4` 64 คลิป · IsolationForest ถูกเลือก 20/20 fold-run · `h4/H4_REPORT.md` |
| **H6 ประเมินผล + รูปโปสเตอร์** | เสร็จ | AUC 0.9885 (CI 0.967–1.000) · รูป A–F 300 dpi · ตาราง 1–5 · `h4/POSTER_NUMBERS.md` |
| **นำขึ้น GitHub + Pages + เตรียม Render** | เสร็จ | repo public · หน้า static ที่ teamnajaa.github.io/ironpulse-zero · `Dockerfile` + `render.yaml` + `baseline_seed.json` · ดู `DEPLOY_RENDER.md` |
| **H5 เว็บแอป** | เสร็จ | `python run.py` · 3 หน้า · เกณฑ์ตรวจรับผ่าน 10/10 · คะแนนตรงกับ H4 ทุกบิต · `web/H5_REPORT.md` |

---

## 3. ลายนิ้วมือของ core — ตรวจก่อนและหลังแก้ทุกครั้ง

**ผลการตรวจสอบ** `synthetic_selftest/selftest_results.csv`
```
md5 = 2702407fa08646e29a257ab99888b051
18 checks, 0 FAIL, 1 expected failure (literal 0.5 Hz high-pass)
```

รันด้วย `python synthetic_selftest/validate_camera_model.py` จากที่ไหนก็ได้
**ถ้า md5 เปลี่ยน แปลว่ามีคนแก้สูตรใน core ต้องหาสาเหตุก่อนไปต่อ**

**md5 ของแต่ละไฟล์ใน `core/`**
```
camera_sim.py  d844aec1994eb028acd6425d743f33ac
config.py      b3e719c08987e6633eb32917e0446645
detectors.py   115ccbb602ef28273722586f016f3c86
dsp.py         9d967db53b6cab14054f3bcc98876bb7
evaluate.py    c498b6460686703841e0f40d5abf45e7
features.py    3ded5bf27cbaab06b602812a1d4d7307
figures.py     5558eb0d959d382379c3be6dc1cccc33
ims.py         e1dab5b5e00e0509a943145aa0c6a07c
pipeline.py    91e334b3a99d833cca1e4e8cf5a34e00
signals.py     8e973bf93098e3a2ce82d852d5410bc5
__init__.py    d41d8cd98f00b204e9800998ecf8427e
```

`core/pipeline.py` คือตัวสร้างชุดข้อมูลของงานวิจัย **ไม่ใช่** pipeline ของแอป
pipeline ของแอปอยู่ที่ `ironpulse/appcore/pipeline.py`

**การแก้บั๊ก G–N (22 ส.ค. 2569) ไม่ได้แตะ `core/` เลย** ตรวจแล้ว md5 ทั้ง 11 ไฟล์ตรงกับตารางข้างบน
และ self-test ยังได้ 18/18 md5 `2702407fa08646e29a257ab99888b051` เหมือนเดิม
ทั้งหมดแก้ใน `ironpulse/appcore/` และ `ironpulse/config.yaml`

`verify/h3_check.py` **รันซ้ำไม่ได้แล้ว** เพราะคลิปรอบแรกถูกลบ จึงตรวจแทนด้วยการเทียบ `pipeline.peak_of()`
กับสูตรเดิมโดยตรงบนสัญญาณทั้ง 6 เส้นของ pilot2 ผลคือ **ต่างกัน 0 พอดีทุกช่อง**
(freq, amp, noise, prominence, runner_up_amp) การรับประกันของ H3 จึงยังอยู่ครบ

---

## 4. ค่าที่วัดมาแล้ว ห้ามอ่านจาก metadata

| ค่า | ค่าที่ใช้ | ที่มา |
|---|---|---|
| true fps | **240.0** | flash reference 5 Hz SNR 1431 · **ยืนยันซ้ำโดยอิสระ**: หลังแก้ audio stretch พีคเสียงอยู่ที่ 199.98 Hz = 4 × 49.995 Hz คือฮาร์มอนิกไฟบ้าน 50 Hz คลาด 0.01 % |
| กล้อง | iPhone 16 Pro · 1080p · HEVC · `.MOV` · มีเสียง | ยืนยันจากไฟล์จริง |
| ทิศภาพ | 1080 × 1920 แนวตั้ง (มี rotation matrix, ffmpeg และ cv2 หมุนตรงกัน) | วัดจากไฟล์ |
| metadata fps | 30.0 (**ผิด ห้ามใช้**) | ระบบเตือนแล้วใช้ 240.0 คำนวณ |
| audio time stretch | **7.991 – 7.994 ≈ 8.0** | วัดครบทั้ง 8 คลิป |
| iOS export trim | 5.5 วินาที/คลิป | ผู้ใช้วัด — **แต่คลิปนำร่องไม่ถูกตัด** (มี 2194–2248 เฟรม) |
| ความเร็วรอบพัดลม | **18.84 – 19.08 Hz ≈ 1130–1145 rpm** | วัดจากการติดตามทั้ง 8 คลิป |
| ขนาดพิกเซล | 42.9 px/cm ที่ 1080p ระยะ 30 ซม. → 1 px = 233 µm | คำนวณจาก FOV · ⚠️ **ของการจัดเฟรมรอบแรกเท่านั้น ใช้กับ `Arise H3 fix` ไม่ได้** ดูข้อ 7.7 |
| **สเกล setup H4** | **2.5692 px/mm = 389.23 µm/px** | วัดจากไม้บรรทัดใน `calibration_ruler/IMG_8379.MOV` ด้วย `h4/read_ruler.py` · ขีดจำกัดการวัด 19.46 µm · ±6.5% จากเปอร์สเปกทีฟ |
| **จำนวนใบพัด** | **3** | **วัดจริง** สเปกตรัมความสว่างในกรอบหน้าพัดลม พีค blade pass 57.0 Hz ÷ f0 18.96 Hz = 3.006 · ฮาร์มอนิก 113.76 Hz = 6× ยืนยันซ้ำ |
| ความเร็วรอบ pilot2 | 18.96 – 19.08 Hz (1138–1145 rpm) | 6 คลิป สเปรด 0.63 % = 1 bin ของ FFT พอดี |

---

## 5. config ของแอปทุกค่า พร้อมที่มา

ไฟล์ `ironpulse/config.yaml` — **ไม่มีคอมเมนต์ในไฟล์ตาม SPEC 14** ที่มาอยู่ตรงนี้และใน `HANDOVER_PARAMS.md`

### capture
| key | ค่า | ที่มา |
|---|---|---|
| `default_fps` | 240.0 | วัดจริง (ข้อ 4) |
| `min_frames` | **1000** | คำสั่งผู้ใช้ (สเปกเดิม 256) |
| `max_frames` | **2000** | คำสั่งผู้ใช้ · ทำให้ทุกคลิปมี df = 0.12 Hz เท่ากัน |
| `fps_mismatch_warn_ratio` | 0.1 | SPEC 5 |
| `reader` | ffmpeg | คำสั่งผู้ใช้ (แก้สเปกข้อ 1) |
| `ios_export_trim_sec` | 5.5 | ผู้ใช้วัด |
| `min_record_sec` | 11.0 | 5.5 + 5.5 |

### tracking
| key | ค่า | ที่มา |
|---|---|---|
| `win_size` 25 · `max_level` 3 · `max_iter` 40 · `epsilon` 0.001 · `subpix_win` 5 | ตาม SPEC 11 เดิม | ยังไม่ได้ tune |
| `roi_margin_px` | 40 | SPEC 11 เดิม |
| `min_eig_threshold` | 1e-4 | SPEC 6.3 |
| `crop_before_pyramid` | true | **พิสูจน์แล้วว่าปลอดภัย** — ปิด crop ให้ผลเท่ากัน (`verify/crop_effect.csv`) |
| `checker_half_px` | 11 | ครึ่งด้านของเคอร์เนลลายตารางหมากรุกที่ใช้หามาร์กเกอร์ · ค่าเดียวกับที่ H4 ใช้หามาร์กเกอร์ครบ 64/64 คลิป |
| `reacquire_radius_px` | 170 | รัศมีค้นหารอบจุดที่ผู้ใช้คลิก · กว้างพอครอบการขยับของกล้องที่วัดได้จริงราว 70 px พร้อมระยะเผื่อสองเท่า |
| `reacquire_min_response` | 8000.0 | เกณฑ์ความแรงของการตอบสนอง · ค่าที่วัดได้จริงในชุด H4 อยู่ที่ 15000–24000 จึงต่ำกว่าครึ่งหนึ่งของค่าที่อ่อนที่สุด |

**หมายเหตุสำคัญ** ขอบ crop จริงไม่ได้ใช้ `roi_margin_px` แต่คำนวณเป็น
`max(roi_margin_px, (win_size//2) × 2^max_level)` = **96 px** ดูบั๊ก B ในหัวข้อ 6

### analysis — วัดมาจาก `research/results/tables/e9_dsp_params.csv` และ `e9c_bands_and_orders.csv`
| key | ค่า | ที่มา / เหตุผล |
|---|---|---|
| `detrend_order` | 2 | e9 — **ไม่มีผลต่าง** (สเปรด ≤0.002 เทียบ sd 0.059) คงค่าเดิม |
| `window_fn` | hann | e9 — none ชนะบน MAFAULDA +0.015 แต่แพ้ CWRU −0.085 |
| `order_tolerance` | **0.08** | e9 — MAFAULDA ดีสุดที่ 0.08 · ⚠️ CWRU ชอบ 0.02 · เลือก 0.08 เพราะพัดลมรอบแกว่งแบบ MAFAULDA |
| `rms_order_min` | **0.10** | e9 — **สเปกเดิม 0.2 เสีย AUC 0.132** ค่าที่กระทบมากที่สุดในทั้งหมด |
| `bands.sub` | **[0.10, 0.80]** | e9c — ขอบล่าง 0.10 ชนะ 0.20 ทุกกรณี (+0.012 ถึง +0.018) |
| `bands.one_x` | [0.80, 1.20] | ตามสเปก |
| `bands.mid` | [1.20, **3.50**] | e9c — 5.00 ได้ AUC สูงกว่าแต่เป็น artifact เพราะทำให้ E_hi ว่าง 100 % บน CWRU ถ้าใช้ 5.00 พัดลมเกิน 1440 rpm จะทำให้กฎ "เสียดสี" ยิงไม่ออก |
| `bands.high` | [3.50, null] | null = nyquist order คำนวณจาก fs/2/f0 |
| `f0_shift_warn` | 0.05 | SPEC 6.6 |
| `min_revolutions` | 12.5 | = 1 / order_tolerance |
| `window_sec` 1.0 · `hop_sec` 0.25 | ตามงานวิจัย | แอปคำนวณ 1 vector ต่อคลิป ไม่ได้ซอยหน้าต่าง |

### features — **ครบ 13 ตัว ไม่ตัดตัวใดเลย** (คำสั่งผู้ใช้)
```
r1 r2 r3 r05 rbp E_sub E_1x E_mid E_hi harm centroid crest kurt
signals       = [d_machine_x, d_machine_y, d_base_y]
model_signals = [d_machine_x, d_machine_y, d_base_y]   -> feature vector 39 ค่า
```
- **ห้ามตัด `d_base`** เป็นช่องทางเดียวที่จับ "ฐานหลวม" ได้ ตัดทิ้ง = fault หายทั้งชนิด
- **ห้ามตัดกลุ่ม orders** ตามผล E4 เดิม เพราะ e9c พิสูจน์แล้วว่า +0.039 เป็น artifact ของการผสมความเร็วรอบ พอจำกัดเฉพาะที่ 3× ยังอยู่ใต้ Nyquist ผลต่างเหลือ **+0.0009**
- **ห้ามตัด `rbp`** ที่ E4 บอกว่าไม่มีผล เพราะทั้ง CWRU และ MAFAULDA ไม่มีใบพัด งานวิจัย **ทดสอบไม่ได้** แต่แอปใช้กับพัดลมที่นับใบได้

### model
| key | ค่า | ที่มา |
|---|---|---|
| `type` | **mahalanobis** | HANDOVER §2 ห้ามตั้ง OCSVM เป็น default ก่อนผ่าน H4 |
| `pca_variance` | 0.95 | งานวิจัย |
| `n_components_max_frac` | **0.25** | คำสั่งผู้ใช้ · `n_components = min(ที่ 0.95, floor(N/4))` · ทดสอบแล้ว N=40 → PCA อยากได้ 25 ถูก cap เหลือ **10** → 4.00 คลิป/องค์ประกอบ สูงกว่างานวิจัยที่ 3.77 |
| `threshold_percentile` | **95** | e9 + เหตุผลสถิติ: เปอร์เซ็นไทล์สูงสุดที่ไม่ต้องคาดคะเนเลยข้อมูล = 100N/(N+1) → N=40 ได้ 97.6 · ที่ 99 ต้องมี N≥99 |
| `threshold_margin` | **1.0** | **ไม่มีที่มา** สเปกเดิมเขียน 1.2 แบบไม่มีหลักฐาน ตั้ง 1.0 ให้ตรงงานวิจัย |
| `min_baseline_clips` | **20** | E1: CWRU 4 คลิป → FAR 0.493 · MAFAULDA 49 คลิป → FAR 0.020 (สเปกเดิม 5 ต่ำเกินไปมาก) |
| `warn_baseline_clips` | 30 | เหตุผล PCA ใน PROTOCOL §5 |
| `random_state` | 42 | |
| `ocsvm` | nu 0.05 · kernel rbf · gamma scale | E3 (0.785 เทียบ mahalanobis 0.715 บนข้อมูลวิจัย) |
| `isolation_forest` | n_estimators 300 | งานวิจัย |

### quality — คำเตือนบนหน้าจอ
| key | ค่า | ที่มา |
|---|---|---|
| `min_a1_over_noise` | **4.16** | E7: กฎ `q ≤ 0.85·A1` + `noise_rms = q/√12` → `A1 ≥ 4.16 × noise_rms` · ตรวจกับข้อมูลจริง MAFAULDA ได้ 4.0–27.5 (รอด) CWRU 0.02 (พัง) |
| `min_peak_prominence` | **1.3** | เพิ่มใหม่ตอน H3 หลังเจอกรณีสเปกตรัมไม่มีพีค (พีค 4 อันดับแรกห่างกัน 2–11 %) |
| `pixels_per_cm_1080p_30cm` | 42.9 | 1920 px / 44.8 cm |

**เกณฑ์แปลงเป็นหน่วยจริง** ที่ความละเอียด flow 0.05 px การสั่นที่ 1× ต้องเกิน **14.0 µm** (ที่ 1080p — งานวิจัยรายงาน 20.7 µm เพราะสมมติ 720p)

### machine
| key | ค่า | ที่มา |
|---|---|---|
| `blade_count` | **3** | วัดจริงจากสเปกตรัมความสว่าง (ข้อ 4) · **ต้องกรอกเสมอ ไม่งั้น `rbp` = 0.0 ตลอดและนั่นแปลว่า "ไม่ได้วัด" ไม่ใช่ "ไม่เปลี่ยน"** |
| `rbp_min_blade_count` | **4** | ชุด order feature มีถึง 3× (`r05` `r1` `r2` `r3`) ใบพัด ≤3 ใบจึงตกลงบน feature ที่วัดอยู่แล้วเสมอ · ตั้งแต่ 4 ใบขึ้นไปจึงพ้นชุดนี้ |

⚠️ พัดลมตัวนี้มี 3 ใบ order ของ blade pass ตรงกับฮาร์มอนิกที่ 3 ของเพลาพอดี ทำให้ `rbp` เท่ากับ `r3`
ทุกตัวเลข ทุกคลิป ทุกแกน (ตรวจแล้ว) **ตอนนี้ระบบปิด `rbp` อัตโนมัติแล้ว** (บั๊ก N)
`cfgmap.rbp_blade_count()` ส่ง `None` เข้า core เมื่อใบพัด < 4 → `avail["rbp"] = False` และกฎใบพัดถูกข้าม
`rbp` ที่อ่านได้ 0.0 จึงแปลว่า **ไม่ได้วัด** เสมอ · การหา f0 จาก blade pass ไม่ถูกปิด ยังใช้ได้ทุกจำนวนใบ

### scale
| key | ค่า | ที่มา |
|---|---|---|
| `calibration_file` | calibration.json | เก็บค่าสอบเทียบต่อ `setup_id` |
| `flow_resolution_px` | 0.05 | ความละเอียดของ optical flow ที่ใช้ตั้งเกณฑ์ 14.0 µm เดิม |
| `setup_id` | null | **ยังไม่มีคลิปสอบเทียบ** ดูข้อ 7.7 |

`appcore/calibrate.py` จะ **ปฏิเสธ** การแปลงพิกเซลเป็นไมโครเมตรเมื่อไม่มีค่าสอบเทียบของ `setup_id` นั้น
หรือเมื่อความละเอียดของคลิปไม่ตรงกับตอนสอบเทียบ ห้ามถอยไปใช้ 42.9 px/cm เงียบ ๆ

### rules — **เปลี่ยนจากตัวคูณเป็น z-score แล้ว**
| key | ค่า | ที่มา |
|---|---|---|
| `z_threshold` | **3.0** | ⚠️ **ยังไม่ยืนยัน ดูข้อ 7.5** · 3 sd ทางเดียว = อัตราเตือนผิด 0.135 % ต่อ feature บน baseline ปกติ · pilot2 วัด z(r1) ได้ +13.1 (แกน x) และ +21.4 (แกน y) จึงเหลือระยะเผื่อ 4–7 เท่า |
| `z_std_floor_frac` | 0.01 | พื้นของ sd = 1 % ของค่าเฉลี่ย กัน z ระเบิดตอน sd ≈ 0 · ต่ำกว่า CV ที่วัดได้ต่ำสุดใน pilot2 (1.5 %) จึงไม่บิดเคสจริง |
| `z_broadband_frac` | 0.8 | กฎ "ทุกค่าเพิ่มพร้อมกัน = ไปตรวจความเร็วรอบก่อน" ยิงเมื่อ ≥80 % ของ feature เกินเกณฑ์ไปทางเดียวกัน |

**ค่าเดิมที่เลิกใช้แล้ว** `r1_ratio 2.0` · `harmonic_ratio 2.0` · `high_band_ratio 2.0` · `blade_pass_change 0.3`
**เหตุผลที่ต้องเลิก** `r1 = A1 / sqrt(ΣA²/2)` มีเพดาน √2 = 1.4142 (คลื่นไซน์บริสุทธิ์)
อัตราส่วนสูงสุดที่เป็นไปได้จึงเท่ากับ 1.4142 ÷ r1 ของ baseline ซึ่ง**ขึ้นกับ baseline ไม่ใช่ขึ้นกับความรุนแรงของ fault**
บน pilot2 แกน x เพดานคือ 1.990 เท่า **ต่ำกว่าเกณฑ์ 2.0 คือกฎเป็นไปไม่ได้ทางคณิตศาสตร์ ไม่ว่าถ่วงน้ำหนักหนักแค่ไหน**
z-score ไม่มีเพดานแบบนี้ และ SPEC §7 ตาราง `baselines` เก็บ `feature_mean_json` กับ `feature_std_json` ไว้อยู่แล้ว
**กฎความไม่สมดุลใช้การจัดอันดับ ไม่ใช่การกีดกัน** (บั๊ก M) เงื่อนไขคือ
`z(r1)` ต้องสูงสุดในกลุ่ม order feature (`r05` `r1` `r2` `r3`) **และ** `z(r1) ≥ z_threshold`
ของเดิมในสเปกคือ "r1 ขึ้น และ r2 r3 ไม่เปลี่ยน" ซึ่งผิด เพราะความไม่สมดุลจริงดัน r2 ขึ้นด้วย
กฎที่เหลือ (หลวม เสียดสี ใบพัด ความเร็วรอบ) ยังเป็นตรรกะเดิมของ SPEC 6.9 เปลี่ยนแค่เกณฑ์เป็น z

โค้ดอยู่ที่ `appcore/rules.py` · ตรวจกับข้อมูลจริงได้ที่ `verify/rules_check.py`

---

## 6. บั๊กที่แก้ไปแล้ว — อย่าแก้กลับ

| # | บั๊ก | ผลถ้าไม่แก้ | แก้อย่างไร |
|---|---|---|---|
| A | **high-pass 0.5 Hz ตามสเปกเดิม** | สิ่งแปลกปลอม 521.4 µm เทียบสัญญาณจริง 50 µm คือใหญ่กว่า 10 เท่า | งานวิจัยใช้ `max(0.5 Hz, 0.1·f0)` · **แอปไม่ต้องมีตัวกรองนี้เลย** เพราะ optical flow ให้การกระจัดตรง ไม่มีการอินทิเกรต ใช้ detrend + ขอบล่าง order 0.10 แทน |
| B | **ขอบ crop แคบเกินไปสำหรับ LK pyramid** | ที่ `win_size 25, max_level 3` หน้าต่างระดับหยาบสุดกิน 200 px ต้องมีขอบ ≥96 px แต่ config ให้ 80 px | `markers.crop_margin()` = `max(roi_margin_px, (win_size//2)×2^max_level)` = 96 px |
| C | **f0 อัตโนมัติดูแค่แกน y** | ได้ 2.055 Hz (123 rpm) แทนที่จะเป็น 19.04 Hz (1142 rpm) เพราะพัดลมแกว่งแกน x | `pipeline.auto_f0()` ค้นทั้งสองแกนเลือกตาม prominence + รับ `f0_range` ได้ |
| D | **`goodFeaturesToTrack` เลือกมุมมั่ว** | จุดที่ได้ห่างจากที่ผู้ใช้คลิก 17–33 px และจุดที่ 3 ไม่ได้อยู่บนมาร์กเกอร์เลย | เอา 8 มุมแรกแล้วเลือกอันใกล้จุดที่คลิกที่สุด → ตอนนี้ห่าง 1.6–4.5 px |
| E | **`05_rpm_from_audio.py` ไม่แก้การยืดเวลา** | รายงานความถี่ต่ำกว่าจริง 8 เท่า แล้วค่าผิดถูกกรอกลง `dataset_log.csv` | `f_true = f_measured × (audio_sec / (n_frames/240))` |
| F | **`core/config.py` hardcode project root** | แอปนำเข้า core แล้ว KeyError ทันที (config แอปไม่มีคีย์ `paths`) | หา root จาก env var → ไล่ขึ้นจาก cwd → fallback `research/` และใช้ `cfg.get("paths", {})` |
| G | **กฎใช้ตัวคูณบนค่าที่มีเพดาน** | `r1_ratio ≥ 2.0` เป็นไปไม่ได้เมื่อ r1 ของ baseline > 0.707 · pilot2 แกน x เพดาน 1.990 เท่า | เปลี่ยนทุกกฎที่เคยใช้ตัวคูณเป็น z-score เทียบ baseline ที่ `appcore/rules.py` (ดูข้อ 5 หัวข้อ rules) |
| H | **`reference_check()` เงียบเมื่อมี 2 มาร์กเกอร์** | `if t.shape[1] < 3: return []` ทำให้ normal_01 ที่มาร์กเกอร์ผนังขยับมากกว่ามาร์กเกอร์เครื่อง (p2p 0.727 เทียบ 0.604 px) ไม่เตือนเลย | ใช้ `ref_index(t)` = 2 ถ้ามี 3 จุด ไม่งั้น 1 · ตรวจทั้ง p2p และ rms หลัง detrend · ทำงานตั้งแต่ 2 จุด |
| I | **quality gate ตรึงแกน y ไว้ตายตัว** | `primary = peak_of(sigs["d_machine_y"])` ทำให้กลุ่มปกติของ setup ใหม่ (สัญญาณอยู่แกน x) โดนเตือน "สเปกตรัมไม่มีพีคชัดเจน" ทั้งที่คลิปดีสมบูรณ์ | `pipeline.primary_axis()` เลือกแกนจาก prominence · และวัด prominence/`a1_over_noise` **ที่ order 1 จริง ๆ** ด้วย `peak_at_order()` ไม่ใช่พีคสูงสุดทั่วสเปกตรัม |
| J | **`a1_over_noise` ไม่ได้วัดที่ 1x** | normal_03 เคยได้ 25.9 จากพีค 3.72 Hz ซึ่งไม่ใช่ความเร็วรอบเลย ผ่านเกณฑ์คุณภาพด้วยเหตุผลผิด | `peak_at_order(sig, fs, cfg, f0, order=1.0)` ค้นเฉพาะใน f0×(1±order_tolerance) · ตอนนี้ normal_03 เตือนถูกต้องว่า 1x เด่นกว่าพีครองแค่ 0.94 เท่า |
| K | **`auto_f0()` ไม่รู้จัก baseline** | normal_03 ถ้าไม่บังคับย่านจะได้ 3.72 Hz แทน 19.08 Hz คือผิด 5 เท่า ซึ่งจะเกิดจริงในแอปเมื่อผู้ใช้ไม่กรอกรอบหมุน | รับ `baseline_f0` · ค้นพีคใน baseline×(1±`f0_shift_warn`) ก่อน โดยคิด prominence เทียบทั้งย่าน · ถ้าไม่เจอพีคที่เด่นพอจึงถอยไปใช้ทั้งย่านแล้ว**เตือนว่าถอย** |
| M | **กฎไม่สมดุลใช้การกีดกัน `r2` `r3` ไม่เปลี่ยน** | ความไม่สมดุลจริงดัน r2 ขึ้นด้วย (z = +5.4 บนแกน x) เงื่อนไข "ไม่เปลี่ยน" จึงเป็นเท็จ กฎไม่ยิงทั้งที่ z(r1) = +13.8 ตรรกะเดิมในสเปกผิด | เปลี่ยนเป็น **การจัดอันดับ**: `z(r1)` สูงสุดในกลุ่ม order feature และ ≥ `z_threshold` · ตอนนี้ยิงครบ 3/3 คลิป unbal ทั้งสองแกน และ 0/3 คลิปปกติ |
| N | **`rbp` คำนวณกับใบพัดน้อยกว่า 4 ใบ** | พัดลม 3 ใบทำให้ order ใบพัดตรงกับ `r3` พอดี `rbp` เลยซ้ำ `r3` ทุกตัวเลข และไปยิงกฎ "ใบพัดเสียหาย" แทนกฎไม่สมดุล (z(rbp) = −9.65) | `cfgmap.rbp_blade_count()` คืน `None` เมื่อใบพัด < `machine.rbp_min_blade_count` → core ตั้ง `avail["rbp"] = False` และ `rules.evaluate()` ข้ามกฎใบพัดให้เอง · แอปเตือนพร้อมเหตุผลทุกคลิป · **ไม่ได้แตะ `core/`** |
| O | **เว็บแอปใช้ตำแหน่งมาร์กเกอร์ชุดเดียวกับทุกคลิป** | ชุด `Arise H4` ถ่ายคนละรอบ กล้องขยับไปราว 70 px มาร์กเกอร์จึงเกาะผิดจุด **A1 ของ IMG_8447 เพี้ยนจาก 22.2 เป็น 32.1 µm** | `markers.reacquire_all()` หาลายตารางหมากรุกในรัศมี `tracking.reacquire_radius_px` รอบจุดที่ผู้ใช้คลิก ก่อนเริ่มติดตาม เป็นวิธีเดียวกับ H4 ผลจึงตรงกับ H4 ทุกบิต · หาไม่เจอจะใช้จุดเดิมแล้วเตือน · เจอแต่ขยับไกลจะเตือนว่ากล้องถูกย้าย |
| P | **`extract_tracks.py` เก็บ track เป็น float32** | ที่พิกัดราว 1000 px ให้ความละเอียด เพียง ~6e-5 px เทียบกับสัญญาณของคลิปปกติที่ 0.042 px คือสูญเสียความละเอียดโดยไม่จำเป็น และทำให้เว็บแอปคำนวณ feature ไม่ตรงกับ H4 (ต่าง 3.1e-04) | เปลี่ยนเป็น float64 แล้วรันโซ่ H4 ใหม่ ตัวเลขขยับเล็กน้อย ข้อสรุปไม่เปลี่ยน และตอนนี้ตรงกันทุกบิต |
| L | **สัญญาณที่ config สั่งไว้หายไปเงียบ ๆ** | `if name not in sigs: continue` ทำให้ไม่มี `d_base_y` แล้ว feature vector หดจาก 39 เหลือ 26 โดยไม่มีใครรู้ · baseline กับคลิปตรวจที่จำนวนมาร์กเกอร์ต่างกันจะเทียบกันแบบผิด ๆ | `pipeline.signal_checks()` เตือนทั้งสัญญาณที่ขาดและกรณี `model_signals` ขาด · `rules.zmap()` โยน `RuleError` เมื่อความยาวเวกเตอร์ไม่ตรงกัน · ยังไม่ได้ระบุ `blade_count` ก็เตือนด้วย |

---

## 7. ข้อค้างที่ยังไม่ตัดสิน — ต้องถามผู้ใช้ ห้ามเดาเอง

### 7.1 ✅ ปิดแล้ว — คลิปนำร่องรอบแรกไม่มีอยู่แล้ว
โฟลเดอร์ `Arise H3` ถูกลบไปแล้ว คำถามว่าคลิปไหนคือดินน้ำมันจึงไม่ต้องตอบอีก
pilot รอบ 2 บอกกลุ่มมาในชื่อไฟล์แล้ว (`normal_` / `unbal_`)
ไฟล์ `verify/pilot_*.*` ของรอบแรกยังอยู่เป็นบันทึกย้อนหลัง แต่รันซ้ำไม่ได้เพราะคลิปหายแล้ว

### 7.2 ✅ ปิดแล้ว — ปัญหาไม่ใช่ดินน้ำมันเบา แต่เป็นเพดานของ r1
pilot2 คือ 6 คลิปใน `Arise H3 fix` ติดตามสำเร็จ 2000/2000 ทุกคลิป

| แกน | r1 ปกติ (sd) | r1 ไม่สมดุล (sd) | อัตราส่วน r1 | แอมพลิจูด 1x ปกติ → ไม่สมดุล |
|---|---|---|---|---|
| **y** (แกนสัญญาณของ setup ใหม่) | 0.1851 (0.0424) | 1.0891 (0.0166) | **5.882 ×** | 0.0118 → 2.4538 px = **207.8 ×** |
| x | 0.7108 (0.0279) | 1.0751 (0.0317) | 1.513 × | 0.0697 → 1.0204 px = 14.6 × |

**สาเหตุที่แท้จริงของข้อ 7.2 เดิม** ไม่ใช่ดินน้ำมันเบาเกินไป แต่เป็นเพดาน √2 ของ r1 (ดูข้อ 5 หัวข้อ rules)
รอบแรก r1_x ของกลุ่มปกติ = 0.569 → เพดานอัตราส่วน 2.49 เท่า แต่มีสัญญาณรบกวนจริงจึงแตะ 2.0 ไม่ถึง
**การเพิ่มน้ำหนักดินน้ำมันไม่มีวันแก้ปัญหานี้ได้ ต้องแก้ที่กฎ** ซึ่งทำไปแล้วในบั๊ก G

**CV ของกลุ่มปกติ** r1_x ลดจาก **16.8 % เหลือ 3.9 %** (A1_x 20.9 % · r1_y 22.9 % · A1_y 26.6 %)
กลุ่มไม่สมดุลนิ่งกว่าอีก r1_x 2.9 % · r1_y 1.5 %
⚠️ n=3 เทียบของเดิม n=8 การลดลงส่วนหนึ่งมาจากจำนวนคลิปที่น้อยลงล้วน ๆ ไม่ใช่ setup ดีขึ้นอย่างเดียว

**Cohen's d 26 feature** ตัวแยกดีสุด `d_machine_y.E_1x` d = 68.0 · `d_machine_y.r1` d = 28.1
|d| ≥ 0.8 มี 22 จาก 26 ตัว · ที่ n=3 ต่อกลุ่ม d ระดับนี้อ่านได้แค่ว่า "สองกลุ่มไม่ทับกันเลย" ไม่ใช่ขนาดผล
รายละเอียดทั้งหมดอยู่ที่ `verify/pilot2_report.log` และ `verify/pilot2_cohens_d.csv`

**ข้อจำกัดที่ยังอยู่ ต้องแก้ตอนถ่าย 40 คลิปจริง**
สองกลุ่มถ่ายคนละเซสชันและกล้องถูกขยับระหว่างนั้น (ระยะกระเบื้องผนัง 502–503 px เทียบ 519 px = สเกลต่างกัน 3.2 %)
กลุ่มจึงไม่ใช่ตัวแปรเดียวที่ต่างกัน — 3.2 % อธิบายอัตราส่วน 208 เท่าไม่ได้ และ r1 ไร้มิติจึงไม่กระทบเลย
แต่ **ตอนถ่ายจริงต้องสลับถ่ายปกติ/ผิดปกติในเซสชันเดียวโดยไม่แตะกล้อง**

**แผนการถ่ายจริง** ผู้ใช้แจ้ง 22 ส.ค. 2569 ว่ากำลังไปถ่าย **152 คลิป** (เดิมวางไว้ 40 คลิป)
ยังไม่ได้บอกสัดส่วนปกติ/ผิดปกติ และยังไม่ได้บอกว่าติดมาร์กเกอร์กี่จุด (ดูข้อ 7.8)

### 7.3 ✅ ตอบแล้ว — ย้ายมาร์กเกอร์อ้างอิงไปติดผนังแล้วไม่ช่วย
สหสัมพันธ์ marker เครื่องกับ marker อ้างอิงใน pilot2 = **−0.138 ถึง +0.351**
เทียบรอบแรก −0.11 ถึง +0.33 คือ **แทบไม่ต่างกันเลย**
ทางเลือก (ก) ของข้อ 7.3 เดิม คือย้ายจุดอ้างอิงไปติดของแข็งที่ไม่สัมผัสพื้น **ทำแล้ว = ติดผนัง และไม่ได้ผล**
สรุปได้ว่าการถ่ายแบบตั้งขาตั้งไม่มีการสั่นร่วมของกล้องให้หักล้างจริง ๆ

- normal_01 มาร์กเกอร์ผนังขยับ **มากกว่า** มาร์กเกอร์เครื่อง (p2p 0.727 เทียบ 0.604 px)
- กลุ่มปกติ p2p ratio เฉลี่ย 1.15 × (rms ratio 1.82–2.76 ×) · กลุ่มไม่สมดุล 15.91 × (rms 16.7–41.7 ×)
- p2p ไวต่อการดริฟต์มาก (unbal_02 mach_x p2p 10.18 แต่ rms 0.840 เท่ากับอีกสองคลิป) **ให้ดู rms เป็นหลัก**

**ยังเหลือให้ตัดสิน** (ข) ให้แอปข้ามการลบจุดอ้างอิงเมื่อสหสัมพันธ์ต่ำกว่าเกณฑ์ หรือ (ค) ปล่อยไว้เพราะจำเป็นตอนถือกล้องหรือมี OIS
ตอนนี้ `markers.reference_check()` ทำงานที่ 2 มาร์กเกอร์แล้ว (บั๊ก H) และเตือน normal_01 ได้ถูกต้อง

### 7.4 ✅ ปิดแล้ว — เปลี่ยนเป็น IsolationForest แล้วใน H5
E3 บนข้อมูลวิจัยบอกว่า OCSVM ดีกว่า mahalanobis (0.785 เทียบ 0.715) **แต่บนวิดีโอจริงผลกลับด้าน**

| โมเดล | เกณฑ์วงในเฉลี่ย (ต่ำ = ดี) | วงในเลือก (ระดับคลิป) |
|---|---|---|
| **IsolationForest** | **0.593 ± 0.074** | **20/20** |
| Mahalanobis | 1.328 ± 0.288 | 0/20 |
| PCA reconstruction | 3.771 ± 0.338 | 0/20 |
| OneClassSVM | **162.3 ± 27.6** | 0/20 |

ยืนยันว่าที่ HANDOVER §2 ห้ามตั้ง OCSVM เป็น default ก่อนผ่าน H4 เป็นการตัดสินใจที่ถูกต้อง
`H5_PROMPT.md` §0 ระบุว่าโมเดลคือ IsolationForest จึงถือว่าผู้ใช้ตัดสินแล้ว
**`config.yaml` เปลี่ยนเป็น `model.type: isolation_forest` และเพิ่ม `model.feature_set: primary` แล้ว**
เว็บแอปใช้ค่านี้ และให้คะแนนตรงกับ `h4/results/raw_scores.csv` ทุกบิต

### 7.5 ⚠️ ค่า k ของกฎ z-score ตั้งไว้ 3.0 แต่ยังไม่ได้ยืนยันกับ baseline จริง
`rules.z_threshold = 3.0` มีที่มาเชิงเหตุผล (ดูข้อ 5) แต่ **ยังไม่ได้ทดสอบกับ baseline ขนาด ≥20 คลิปตามที่ config ต้องการ**
สิ่งที่ทดสอบได้ตอนนี้คือ smoke test ด้วย baseline 3 คลิป (`verify/rules_check.log`)
ผล: กลุ่มปกติ **0/13 feature เกิน 3 sd ทั้งสองแกน** (ไม่มีเตือนผิด) · กลุ่มไม่สมดุล z(r1) = +13.8 (x) และ +21.4 (y)
**ต้องยืนยันซ้ำเมื่อมี baseline จริง 20–40 คลิป** ถ้าอยากได้ k ค่าอื่นเปลี่ยนที่ `config.yaml` อย่างเดียว ไม่ต้องแก้โค้ด

### 7.6 ✅ ปิดแล้ว — กฎวินิจฉัยความไม่สมดุลได้แล้ว
สองเหตุที่เคยทำให้กฎยิงไม่ออก แก้ครบทั้งคู่แล้ว (บั๊ก M และ N)

| เหตุเดิม | แก้เป็น |
|---|---|
| เงื่อนไข "r2 r3 ไม่เปลี่ยน" เป็นเท็จ เพราะความไม่สมดุลจริงดัน r2 ขึ้นด้วย (z = +5.4) | ใช้**การจัดอันดับ** แทนการกีดกัน: `z(r1)` สูงสุดในกลุ่ม order feature และ ≥ `z_threshold` |
| `rbp` ≡ `r3` (พัดลม 3 ใบ) ไปยิงกฎ "ใบพัดเสียหาย" แทน | ปิด `rbp` อัตโนมัติเมื่อใบพัด < `machine.rbp_min_blade_count` = 4 พร้อมข้ามกฎใบพัด |

**ผลบน pilot2** (`verify/rules_check.log`) กฎ `ความไม่สมดุลของมวลหมุน` ยิงครบ **3/3 คลิป unbal ทั้งสองแกน**
และ **0/3 คลิปปกติไม่เข้ากฎใดเลย** ทั้งสองแกน

| คลิป | แกน x | แกน y |
|---|---|---|
| unbal_01 | z(r1) = +13.77 · รองลงมา z(r2) = +4.15 | z(r1) = +21.39 · รองลงมา z(r2) = −0.42 |
| unbal_02 | z(r1) = +13.66 · รองลงมา z(r2) = +5.39 | z(r1) = +21.68 · รองลงมา z(r2) = −0.27 |
| unbal_03 | z(r1) = +11.75 · รองลงมา z(r2) = +4.63 | z(r1) = +20.90 · รองลงมา z(r2) = +0.49 |

⚠️ ยังเป็น baseline 3 คลิปอยู่ ต้องยืนยันซ้ำเมื่อมี baseline จริง (ดูข้อ 7.5)
กฎที่เหลือ (หลวม เสียดสี ความเร็วรอบ) ยังไม่ได้ทดสอบเพราะ pilot2 ไม่มี fault ชนิดนั้น


### 7.7 ✅ ปิดแล้ว — สอบเทียบสเกลสำเร็จ
`calibration_ruler/IMG_8379.MOV` มีไม้บรรทัดเหล็กในเฟรม อ่านได้ด้วย `h4/read_ruler.py`

| | |
|---|---|
| วิธี | fit ขอบไม้บรรทัด (มุม −4.523° · inlier 425/446 · rms 0.51 px) แล้วหาคาบขีดสเกลด้วย autocorrelation |
| ยืนยัน | คาบตรงกันทุก lag k = 1…11 (25.5–25.9 px ต่อขีด) |
| **ผล** | **2.5692 px/mm = 389.23 µm/px** · เก็บที่ `ironpulse/calibration.json` setup_id `h4_wall_2026_08_22` |
| ขีดจำกัดการวัด | 0.05 px = **19.46 µm** |
| ความไม่แน่นอน | ครึ่งซ้ายกับครึ่งขวาต่างกัน **6.48%** จากเปอร์สเปกทีฟ |

⚠️ การจัดเฟรมชุดนี้**หยาบกว่าชุดเดิม 1.67 เท่า** (389 µm/px เทียบ 233 µm/px ของ 42.9 px/cm เดิม)
เป็นสาเหตุโดยตรงที่คลิปถ่วงมวลเบา 4 คลิปตรวจไม่พบ ดู `h4/H4_REPORT.md` ข้อ 8

### 7.8 ✅ ปิดแล้ว — มาร์กเกอร์จุดที่ 3 กลับมาแล้ว
ชุด `Arise H4` ติดครบ 3 จุด (หัวมอเตอร์ · ฐาน · ผนัง) feature vector กลับมาเป็น **39 ค่า**
วัดได้จริง 36 ค่า เพราะ `rbp` ทั้ง 3 สัญญาณถูกปิดตามบั๊ก N (พัดลม 3 ใบ)

### 7.9 ⚠️ คลิปปกติกระจุกอยู่ในรอบถ่ายเดียว — กระทบตัวเลขมากที่สุด
คลิปปกติ 40 คลิปอยู่ใน 4 รอบการถ่าย แต่กระจายไม่เท่ากันเลย: **S1 = 26 · S2 = 5 · S3 = 4 · S4 = 5**

ผลคือ **GroupKFold k=5 ตามสเปกทำไม่ได้** (มีแค่ 4 กลุ่ม) ต้องใช้ leave-one-session-out k=4
และเมื่อกัน S1 ไว้ทดสอบ เหลือคลิปฝึกแค่ **14 คลิป ต่ำกว่า `min_baseline_clips: 20` ของระบบเอง**
fold นั้นจึงตรวจไม่พบอะไรเลย (AUC 0.769 · อัตราตรวจพบ 0.000 ทั้งสองระดับ)
ขณะที่อีกสาม fold ได้ AUC 1.000 / 1.000 / 0.997

**ตัวเลขพาดหัวคือค่าเฉลี่ยทุก fold: AUC 0.9413 ± 0.1030** ส่วนเบี่ยงเบนที่ใหญ่มาจาก fold เดียวนี้
**วิธีแก้อยู่ที่การเก็บข้อมูล ไม่ใช่โมเดล** ถ่ายคลิปปกติให้กระจายอย่างน้อย 5 รอบ รอบละ ~8 คลิป

### 7.10 ⚠️ ความเร็วรอบต่างกันระหว่างกลุ่ม เกินเกณฑ์ 5% ของระบบเอง
ปกติ 18.84–19.08 Hz · ต่ำกว่า 3 ก. **−1.53%** · 3 ก. **−4.84%** (18.00–18.12 Hz) · สเปรดรวม **5.73%**
เกิน `f0_shift_warn: 0.05` การถ่วงมวลทำให้พัดลมช้าลงจริงตามฟิสิกส์ จึงเป็นส่วนหนึ่งของลายเซ็นความผิดปกติ
**แต่แยกผลของ f0 ออกจากผลของแอมพลิจูดไม่ได้ด้วยชุดข้อมูลนี้**
ถ้าจะแยก ต้องถ่ายคลิปปกติที่ความเร็วรอบเดียวกับกลุ่ม fault เพิ่ม

### 7.11 ⚠️ ไม่มีบันทึกมวลรายคลิป และโฟลเดอร์ "<3 กรัม" มีสองน้ำหนักปนกัน
ไม่มีไฟล์บันทึกมวลเลยในชุด `Arise H4` (ไม่มีไฟล์ที่ไม่ใช่ `.MOV`) จึงมีระดับความรุนแรงแค่ 2 ระดับ
**ห้ามเดามวล** และทำกราฟความไวเทียบ g·mm ไม่ได้ (ไม่มีทั้งมวลและรัศมีติดตั้ง)

แต่แอมพลิจูดบอกว่ากลุ่ม "<3 กรัม" แยกเป็นสองก้อนชัดเจน

| ก้อน | คลิป | A1 | f0 |
|---|---|---|---|
| เบา | 8444–8451 (7 คลิป) | 21.1 – 27.3 µm | 18.84 – 19.08 Hz |
| หนัก | 8453–8460 (9 คลิป) | 62.0 – 136.4 µm | 18.48 – 18.60 Hz |

**ครั้งหน้าต้องจดมวลทุกคลิป** จะได้ 3 ระดับความรุนแรงและทำกราฟความไวได้ทันที

### 7.12 ⚠️ กล้องถูกขยับระหว่างรอบการถ่าย (เรื่องเดิมของ 7.2 ยังไม่หาย)
มาร์กเกอร์ผนังตรึงกับโลกจริงแต่ตำแหน่งในภาพขยับสูงสุด ~70 px ระหว่างกลุ่มปกติกับกลุ่ม fault
สเกลการจัดเฟรมต่างกันราว **3%** ซึ่งอธิบายอัตราส่วนแอมพลิจูด 59 เท่าไม่ได้ และ feature เป็นอัตราส่วนไร้มิติเกือบทั้งหมด
แต่ยังเป็นตัวกวนที่ต้องบันทึก **ครั้งหน้าต้องสลับถ่ายปกติ/ผิดปกติในรอบเดียวกันโดยไม่แตะกล้อง**


---

### 7.13 ⚠️ การยึด f0 กับ baseline อาจล็อกพลาดไปหนึ่ง bin
`analysis.use_baseline_f0` เปิดอยู่ ระบบหาพีคในช่วง baseline × (1 ± `f0_shift_warn`) ก่อน
คลิป 3 กรัมหมุนที่ 18.12 Hz ส่วน baseline อยู่ที่ 19.08 Hz ขอบล่างของหน้าต่างคือ 18.126 Hz
**18.12 จึงหลุดนอกหน้าต่างหวุดหวิด** ระบบล็อกที่ 18.24 Hz ห่างไปหนึ่ง bin (0.66%)

ผลกระทบจริงเป็นศูนย์ในเคสนี้ เพราะ `order_tolerance` 0.08 กว้างพอให้ A1 ยังวัดที่พีคจริง
(946.26 µm ตรงกับ H4 เป๊ะ) คะแนนและคำตัดสินถูกต้อง **แต่ค่า f0 บนหน้าจอคลาดไปหนึ่ง bin**

**ต้องตัดสิน** (ก) ขยายหน้าต่างเป็น `max(f0_shift_warn, 3 bins)` (ข) ถ้าพีคย่านกว้างเด่นกว่ามาก ให้ใช้พีคย่านกว้างแล้วเตือน (ค) ปิด `use_baseline_f0` แล้วรับความเสี่ยงของบั๊ก K แทน

---

## 8. เอกสารที่ต้องแก้ก่อนเก็บข้อมูลจริง

| เอกสาร | ปัญหา | ต้องแก้เป็น |
|---|---|---|
| `DATA_COLLECTION_PROTOCOL.md` §4 | เขียนว่า "แต่ละคลิปยาว 5 วินาที" และ "ถ่ายกำแพงเปล่า 3 วินาที" — **iOS ตัด 5.5 วินาที จะได้ 0 เฟรม** | **อัดคลิปละ 11 วินาที** ทุกคลิปรวมคลิปเช็คแสง |
| `DATA_COLLECTION_PROTOCOL.md` §5 | ประเมินพื้นที่ 4 GB จากคลิป 5 วินาที | ประมาณ **9 GB** ที่ 11 วินาที |
| `DATA_COLLECTION_PROTOCOL.md` §6 | ขั้นตอนแปลง HEVC → H.264 ทั้งโฟลเดอร์ | **ไม่ต้องใช้แล้ว** ffmpeg pipe อ่าน HEVC ตรงได้ |
| `SPEC_ironpulse_zero.md` §5 | เขียน 720p และ H.264 | ของจริง **1080p HEVC** |
| `SPEC_ironpulse_zero.md` §12 | ตั้งเป้าจาก 720p 1200 เฟรม | ของจริง 1080p · วัดได้ **6.3 วินาทีต่อ 1200 เฟรม** ผ่านเป้า 10 วินาที |
| `SPEC_ironpulse_zero.md` §11 | `min_frames 256` · `rms_order_min 0.2` · `threshold_percentile 99` · `threshold_margin 1.2` · `min_baseline_clips 5` | เขียนทับด้วยค่าในหัวข้อ 5 ทั้งหมด |
| `SPEC_ironpulse_zero.md` §6.9 | เกณฑ์ทุกข้อเป็น **ตัวคูณ** ("r1 เพิ่มเกิน 2 เท่า") และปิดท้ายว่า "เกณฑ์ตัวคูณทั้งหมดต้องอยู่ใน config" | **เปลี่ยนเป็น z-score เทียบ baseline** (บั๊ก G) เพราะ r1 มีเพดาน √2 ทำให้เกณฑ์ตัวคูณเป็นไปไม่ได้บนบางแกน |
| `SPEC_ironpulse_zero.md` §6.9 กฎไม่สมดุล | `r1 เพิ่มเกิน 2 เท่า และ r2 r3 ไม่เปลี่ยน` | **ตรรกะผิด** ความไม่สมดุลจริงดัน r2 ขึ้นด้วย เปลี่ยนเป็น `z(r1)` สูงสุดในกลุ่ม order feature และ ≥ `z_threshold` (บั๊ก M) |
| `SPEC_ironpulse_zero.md` §6.9 กฎใบพัด | `rbp เปลี่ยนเกิน 30%` | ใช้ z เหมือนกฎอื่น · และ**ปิดกฎอัตโนมัติเมื่อใบพัด < 4** เพราะ order ใบพัดชนฮาร์มอนิกเพลา (บั๊ก N) |

---

## 9. คำสั่งที่ใช้บ่อย

```bash
cd C:\Users\more_\Downloads\Arise

python synthetic_selftest\validate_camera_model.py        # ต้องได้ 18/18 md5 2702407f...
cd ironpulse
python verify\pilot2_extract.py                           # ประมวลผล 6 คลิป pilot2 ~47 วินาที
python verify\pilot2_report.py                            # ตาราง + Cohen's d + รูป
python verify\rules_check.py                              # กฎ z-score บนข้อมูล pilot2

python h4\inventory.py                                    # สำรวจ Arise H4 + อ่าน creation_time
python h4\read_ruler.py                                   # px/mm จากคลิปไม้บรรทัด
python h4\extract_tracks.py                               # ติดตาม 64 คลิป ~8 นาที
python h4\features_from_tracks.py                         # feature ระดับคลิปและระดับหน้าต่าง
python h4\run_nested_cv.py                                # nested CV ~9 นาที
python h4\evaluate.py                                     # ตัวชี้วัด + รูป A-F + ตาราง 1-5

python web\seed_demo.py                                   # ตั้งค่าเริ่มต้นครั้งเดียว ~4 นาที
python run.py                                             # เปิดเว็บแอป
python webcceptance.py                                  # เกณฑ์ตรวจรับ 10 ข้อ (ต้องเปิดเซิร์ฟเวอร์ค้างไว้)
python h4erify_webapp.py                                # พิสูจน์ว่าเว็บแอปให้คะแนนตรงกับ H4

python webaseline_seed.py                               # ส่งออก baseline เป็นไฟล์ seed สำหรับ Docker
python webaseline_seed.py import                        # นำเข้า baseline เข้าฐานข้อมูลว่าง
python webuild_pages.py TEAMNAJAA ironpulse-zero        # สร้างหน้า static ใน docs/
```

**สอบเทียบสเกล** (ข้อ 7.7) ทำสองขั้น ขั้นแรกดึงเฟรมออกมาหาพิกัดบนไม้บรรทัด ขั้นสองบันทึกค่า

```bash
python verify\calibrate_scale.py --clip <ruler.MOV> --setup-id <ชื่อ> --dump-frame frame.png
python verify\calibrate_scale.py --clip <ruler.MOV> --setup-id <ชื่อ> --pair x1,y1,x2,y2,mm
```

**สคริปต์ที่รันไม่ได้แล้ว** `verify\h3_check.py` และ `verify\pilot_extract.py` ชี้ไปที่โฟลเดอร์ `Arise H3`
ซึ่งถูกลบไปแล้ว เก็บไว้เป็นบันทึกย้อนหลังเท่านั้น ผลเดิมอยู่ใน `h3_results.csv` และ `pilot_raw.csv`

---

## 10. กติกาที่ห้ามละเมิด

- ห้ามเขียน `core/` ขึ้นใหม่ · ถ้าแก้ต้องรัน self-test ซ้ำและบันทึกว่ายังผ่าน 18/18
- ห้ามเอาค่า AUC ของงานวิจัย (0.715 / 0.785) ขึ้นหน้าจอแอป — มาจากมาตรวัดความเร่ง ไม่ใช่กล้อง
- ห้ามตั้ง OneClassSVM เป็น default ก่อนผ่าน H4
- ห้ามใส่คอมเมนต์ในโค้ด · ห้าม CDN · ห้าม build tool · ห้าม Docker · ห้ามสร้างข้อมูลปลอม
- ห้ามใช้ metadata fps คำนวณ ใช้ 240.0 เสมอ
- ห้ามแก้ `03_vibro.py` — เป็นตัวอ้างอิงของ H3 (ถ้าต้องแก้ ให้แก้สำเนาที่ `ironpulse/verify/vibro_reference.py`)

---

NEXT ACTION: deploy บน Render ตาม `DEPLOY_RENDER.md` แล้วตัดสินว่าจะให้มีคลิปสาธิตบนเน็ตหรือไม่ ซึ่งมี 3 ทางเลือกพร้อมข้อแลกเปลี่ยนอยู่ใน DEPLOY_RENDER ข้อ 5 (บีบอัดคลิปแล้วตัวเลขจะไม่ตรงกับ H4 · เพิ่มโหมดเล่นซ้ำจาก track ที่ข้ามการถอดรหัสวิดีโอ · ใช้ persistent disk ซึ่งเสียเงิน) · ยังค้างอีก 3 ข้อจากก่อนหน้า คือ ยืนยัน `rules.z_threshold` = 3.0 กับ baseline จริง (7.5) · เลือกทางแก้การยึด f0 ที่ล็อกพลาดหนึ่ง bin (7.13) · ถ่ายข้อมูลเพิ่มโดยทำ 5 อย่างพร้อมกันตามข้อ 7.9–7.12 ซึ่งคุ้มที่สุด · ถ้าถ่ายเพิ่มแล้ว รัน `h4/extract_tracks.py` → `features_from_tracks.py` → `run_nested_cv.py` → `evaluate.py` → `web/seed_demo.py` → `web/baseline_seed.py` → `web/build_pages.py` แล้ว push ก็อัปเดตทั้งระบบและหน้าเว็บพร้อมกัน
